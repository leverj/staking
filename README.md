# LEV Stake Contract

Make sure to install the required dependencies with `npm install`

### Prerequisites:

```
truffle 3.4.5
node 8.4.0
```

### build
```
    npm install
    truffle compile
    npm test
```
